# bza-dayz-site (retired)

The BZA Chernarus 1PP site now lives at **https://bzadayz.com** on our own
server. This repository is kept only so that links published before the move
keep working: every page here redirects to its counterpart on the new domain.

Do not edit these files. The real site is in the private `bza-dayz` repo under
`site/`, deployed with `scripts/deploy_site.ps1`.
